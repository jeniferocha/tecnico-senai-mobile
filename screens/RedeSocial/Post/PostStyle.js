import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    padding: 30,
  },
  textoPerfil: {
    fontSize: 16,
  },
});

export default styles;
